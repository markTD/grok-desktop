# Grok Desktop does not require app-level API keys for ACP (uses the installed `grok` CLI).
# This file is only a placeholder so .env patterns stay gitignored if you add local tooling later.

# GROK_BINARY=/path/to/grok
